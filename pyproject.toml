[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
[project]
name = "servora"
version = "0.1.25"
description = "Lightweight Podman homelab manager"
requires-python = ">=3.10"
[tool.pytest.ini_options]
pythonpath = ["."]
